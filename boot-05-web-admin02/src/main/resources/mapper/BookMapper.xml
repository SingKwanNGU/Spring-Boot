<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.boot.mapper.BookMapper">
<!--    getBook(Integer bookId)-->
    <select id="getBook" resultType="com.atguigu.boot.bean.Book">
        select * from t_book where book_id=#{bookId}
    </select>
</mapper>